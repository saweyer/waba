TO DO (maybe)? MIME types for .wrp, "init" files? check for main class (via "extends MainWindow" bytecode); javap subset? other validity checks (see Warp)WabaTester changes.txt1.0o (2/12/01):- recognizes .pkg generated by Warp/Exegen- reduces class entry storage: binary vs. {class: binary, offset: 0, length: #};  :getWabaClass creates frame entry if necessary- adds zoom capability (tap image in pop window to increment scale)- for NTK format, lists both icon and iconPro- [NTK lib] sets icon at build (rather than install) time [thanks Paul]- [NTK app] fixed invert for some icons1.0n (1/15/01):- makes icon an "old" bitmap (to display in Extras on NOS 2.0)- adds title to args; more args displayed in info- keeps prefs when shutting down saved .pkg- adds/checks appRequires; eliminates appArgs.archive- defers button actions to avoid hilite/error problems- prints current app prefs (if any) with Save(Pref); all if no app selected (e.g., initially)- [NTK app] sets icon at build (rather than install) time [thanks Paul]- [apps] shortened titles, added MyWabaApp.fix.txt to increase heap prefs/fix old icons- [lib/waba] setAppContext uses appRequires1.0m (1/4/01):- checks each class for signature (kClassSig)- supports gray scale for (non-icon) .bmp; supports .wbm- substitutes generic Waba app icon for mostly black default (i.e., skey)- accesses setAppContext via lib/waba (bug in saved 1.0l .pkgs)- helperApp passes appArgs (if any) 1.0l (12/28/00):- changes Hex choice to NTK and saves in template .cls.txt, .arg.txt format- setAppContext defined/used in lib/waba (and not in each app)- copies (rather than ors) "W" into icon1.0k (12/27/00):- convertBitmap rewritten w/o certain run-time only functions (reusable by WabaNewApp)- NewtPack included in distribution- fixes readonly error in args: clones so uniqueName,width,height defaults can be fixed in old apps- extracts args and icon.bmp from .prc file- DeletionScript removes prefs entry directly (since WabaVM might not be installed)1.0j (12/24/00):- fixes border invert for bitmaps- open/close: removed app icon change- open: Notify if WabaVM or lib/waba not present- uses Sean's stylized "W" at bottom right rather than "WABA" at top for app/lib/tester icons & icon.bmp - uses appMainName for package symbol (i.e., with path, instead of possibly shorter/duplicate appName)- adds support to browse NTK-generated .pkg- adds DeletionScript for apps; RemovePreferences in appShutdown-- (lib/waba fixes MainWindow.defaultFont = "NewtonDefault" (& original size: 12);1.0i (12/21/00):- uses MakeBitmap (instead of older 1.x compat format)- changes arg nativestacksize to nmstacksize- uses kSetExtrasInfoFunc for compat with older MP- (lib/waba fixes MainWindow.defaultFont = "Espy",...,10;	classes smaller via javac -g:none to remove debug info;	waba/lang/... paths duplicate as java/lang/...)1.0h (12/10/00):- fixes problem with archive arg in setAppContext- VMExited causes :close for standalone app- changes standalone app icon during :open and :close1.0g:- adds MIME for .class,.jar- uses icon.bmp in .jar for app icon, appname.htm for args- eliminates appArgs.appMainName- consolidates 2*4 arrays to builtinFiles,inboxFiles, pkgFiles- recognizes args in .htm for .class- adds Packages back to Apps picker; sort- click on .bmp name to display icon; click on icon to see complete in popup- defers launchVM/closeVM to avoid highlight/init problems- flips bitmap vert (not horiz) & inverts1.0f:- modifies appLaunch, added appShutdown, VMExited- changes appArg slot names1.0e:- adds helperApp- flips bitmap horiz; "WABA" automatic header for icon.bmp